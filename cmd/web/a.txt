CREATE DATABASE scrap CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

use scrap;

CREATE TABLE scraps (
id INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT,
email VARCHAR(100) NOT NULL,
created DATETIME NOT NULL,
expires DATETIME NOT NULL
);

CREATE INDEX idx_snippets_created ON scraps(created);


INSERT INTO scraps (email,created, expires) VALUES (
'amitwpu@gmail.com',
UTC_TIMESTAMP(),
DATE_ADD(UTC_TIMESTAMP(), INTERVAL 365 DAY)
);

INSERT INTO scraps (email,created, expires) VALUES (
'amitwpu2019@gmail.com',
UTC_TIMESTAMP(),
DATE_ADD(UTC_TIMESTAMP(), INTERVAL 365 DAY)
);


CREATE USER 'test'@'localhost';

GRANT SELECT, INSERT, UPDATE ON snippetbox.* TO 'test'@'localhost';

ALTER USER 'test'@'localhost' IDENTIFIED BY 'pass';

SELECT id, email, expires FROM scraps;-------------->testing
